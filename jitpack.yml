jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.14-amzn
  - sdk use java 17.0.14-amzn
install:
  - ./gradlew clean -Pgroup=com.github.last9 -Pversion=$VERSION -xtest -xlint assemble publishToMavenLocal
